// Public site origin - used to build "view on the live site" style links
// (currently: newsletter recommendation CTA URLs) from the admin SPA.
export const SITE_URL = import.meta.env.VITE_SITE_URL || "http://localhost:5173";
